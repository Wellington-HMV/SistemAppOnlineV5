@model sistemav5.Models.ViewModels.PedidoViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Pedido</h4>
<hr />
<div class="row">
    <div class="col-md-4">

        <form asp-action="Create">

            <div asp-validation-summary="All" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="Clientes" class="control-label"></label>
                <select asp-for="Clientes" asp-items="@(new SelectList(Model.Clientes,"Id","Nome"))" class="form-control"></select>
            </div>
            <div class="form-group">

                @*count list: @Model.Clientes.Count
                <select>
                    @foreach (var item in Model.Clientes)
                    {
                        <option value="@item.IdCliente">@item.Nome</option>
                    }

                </select>*@


            </div>
            <div class="form-group">
                <label asp-for="Produtos" class="control-label"></label>
                <select asp-for="Produtos" class="form-control" asp-items="@(new SelectList(Model.Produtos,"Id","Nome"))"></select>
            </div>
            @*<div class="form-group">
                    <label asp-for="ClienteId" class="control-label"></label>
                    <input asp-for="ClienteId" class="form-control" />
                    <span asp-validation-for="ClienteId" class="text-danger"></span>
                </div>*@
        <div class="form-group">
            <input type="submit" value="Create" class="btn btn-primary" />
            <a asp-action="Index" class="btn btn-warning">Voltar</a>
            <a asp-action="AdicionarProdutos" class="btn btn-success">Adicionar Produtos</a>
        </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
